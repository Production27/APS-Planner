node_modules/
.wrangler/
test-results/
playwright-report/
dist/
worker/worker-configuration.d.ts
# A working scratch file an AI assistant may write for itself — not
# documentation for a human contributor, so never committed.
SESSION_HANDOFF.md
